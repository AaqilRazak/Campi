Module main

Functions

change_password(request: main.PasswordChangeRequest, db: aiosqlite.core.Connection = Depends(get_db))
	•	Description: Allows a user to change their password.
	•	Request Body:
	•	username (str): The user’s username.
	•	old_password (str): The user’s current password.
	•	new_password (str): The new password the user wants to set.
	•	Response:
	•	200 OK: Password updated successfully.
	•	401 Unauthorized: Invalid credentials.
	•	500 Internal Server Error: Failure during password change.

create_session(session: main.SessionCreate, db: aiosqlite.core.Connection = Depends(get_db))
	•	Description: Creates a new session for a user.
	•	Request Body:
	•	device_type (str, optional): Type of device used.
	•	browser_agent (str, optional): Browser agent string.
	•	Response:
	•	200 OK: Created session ID.
	•	Schema: { "sessionId": <int> }

delete_session(session_id: int, db: aiosqlite.core.Connection = Depends(get_db))
	•	Description: Deletes a session and its associated messages.
	•	Parameters:
	•	session_id (int): The ID of the session to delete.
	•	Response:
	•	200 OK: Confirmation of deletion.
	•	Schema: { "status": "success", "message": "Session <session_id> deleted successfully" }

generate_text(prompt_request: main.PromptRequest, db: aiosqlite.core.Connection = Depends(get_db))
	•	Description: Generates a response using the LLM based on a user prompt.
	•	Request Body:
	•	prompt (str): The user’s prompt.
	•	Response:
	•	200 OK: LLM or fallback database response.
	•	Schema: { "response": <str> }

get_db()
	•	Description: Provides a connection to the SQLite database.
	•	Usage: Inject this function as a dependency for database access.

get_session_messages(session_id: int, db: aiosqlite.core.Connection = Depends(get_db))
	•	Description: Retrieves all messages from a specified session.
	•	Parameters:
	•	session_id (int): The ID of the session.
	•	Response:
	•	200 OK: List of messages.
	•	Schema:

{
    "messages": [
        {
            "MessageID": 1,
            "Timestamp": "2024-12-10T10:00:00",
            "Sender": "User",
            "MessageText": "Hello!"
        }
    ]
}

get_sessions(db: aiosqlite.core.Connection = Depends(get_db))
	•	Description: Retrieves all sessions for the test user.
	•	Response:
	•	200 OK: List of sessions.
	•	Schema:

{
    "sessions": [
        {
            "SessionID": 1,
            "SessionStartTime": "2024-12-10T10:00:00",
            "FirstMessage": "Hello, world!"
        }
    ]
}

get_users(db: aiosqlite.core.Connection = Depends(get_db))
	•	Description: Fetches all users for debugging purposes.
	•	Response:
	•	200 OK: List of users.
	•	Schema:

{
    "users": [
        {
            "id": 1,
            "username": "student",
            "role": 1,
            "firstName": "Student",
            "lastName": "User"
        }
    ]
}

init_db()
	•	Description: Initializes the SQLite database and its tables.
	•	Usage: Automatically called during the application startup event.

init_default_users()
	•	Description: Creates default users in the database if they do not already exist.
	•	Usage: Automatically called during the application startup event.

login(login_request: main.LoginRequest, db: aiosqlite.core.Connection = Depends(get_db))
	•	Description: Authenticates a user by their username and password.
	•	Request Body:
	•	username (str): The user’s username.
	•	password (str): The user’s password.
	•	Response:
	•	200 OK: User information.
	•	Schema: { "id": <int>, "username": <str>, "role": <str>, "firstName": <str>, "lastName": <str> }
	•	401 Unauthorized: Invalid credentials.
	•	500 Internal Server Error: Login failure.

save_message(session_id: int, message: main.MessageCreate, db: aiosqlite.core.Connection = Depends(get_db))
	•	Description: Saves a new message in a session.
	•	Parameters:
	•	session_id (int): The ID of the session.
	•	Request Body:
	•	message (str): The message text.
	•	sender (str): Who sent the message (User or Bot).
	•	timestamp (str): The time of the message.
	•	Response:
	•	200 OK: Success confirmation.
	•	Schema: { "status": "success" }

startup_event()
	•	Description: Handles application startup tasks, such as initializing the database and default users.

Classes

LoginRequest
	•	Attributes:
	•	username (str): The user’s username.
	•	password (str): The user’s password.

MessageCreate
	•	Attributes:
	•	message (str): The text of the message.
	•	sender (str): Indicates who sent the message (User or Bot).
	•	timestamp (str): The time of the message.

PasswordChangeRequest
	•	Attributes:
	•	username (str): The user’s username.
	•	old_password (str): The user’s current password.
	•	new_password (str): The new password.

PromptRequest
	•	Attributes:
	•	prompt (str): The user’s prompt.

SessionCreate
	•	Attributes:
	•	device_type (Optional[str]): Type of device used.
	•	browser_agent (Optional[str]): Browser agent string.